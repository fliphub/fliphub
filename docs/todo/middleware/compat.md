- [ ] ensure full compatibility with webpack configs, not just building the params

## compat
- [ ] if using a config, adjust defaults

- [x] multiple entry points

- [ ] output with hashtag for library target?
