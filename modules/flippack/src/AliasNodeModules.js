// @TODO: pass in root path somehow with interactive, then alias
const paths = require('../../../paths')
const getDirectories = require('flipfile')

const {CLIENT_NODE_MODULES} = paths

console.log(paths)
// const modules = getDirectories(CLIENT_NODE_MODULES)
// console.log(modules)
