// take fusebox and webpack init configs here put them in standard
// as simple first step
const UnifyTranslator = {
  // test: app => app.alias || !app._alias,
  translate: ({app, lib, context}) => {

  },
}

module.exports = UnifyTranslator
