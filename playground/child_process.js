// roots = execFileSync('node', ['execd.js', '--example']).toString()
// console.log(roots)
// log.data(execa).tosource(false).echo()
// .split('/')
// roots = roots.slice(0, roots.length - 1).join('/')
//
// const execd = require('path').resolve(__dirname, './execd.js')
// roots = execa.sync(execd)
// roots = spawnSync('./execd.js')
// roots = execSync(__dirname + './execd.js')
// roots = execFileSync(execd)

// roots = execa.shellSync('npm', ['bin'])
// const eh = execSync('npm bin').toString()
