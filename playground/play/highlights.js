import {moose as mooses} from 'meese'
const meeses = require('geese')

export interface Canada {
  canada: boolean | /(?:regex)[*](it-up)/gmi,
}
export type Eh = 'eh' | Fun | any

let zoo: string = `ooooo`
const lala = (): string => zoo
var aReallyReallyLongVariableName = lala(zoo)

class Wubs {
  static init() { return new Wubs(...{}); }
}

class Skoola extends Wubs  {
  from(): Canada {
    if (0 === 0 && true !== false) {
      this.eh = {
        Canada: true,
        nike: justDoIt(0, 1),
        strings: 'strings on strings on strings',
      }
    }

    return this.eh
  }
}

function justDoIt(a: number, b: number): number {
  return a - b + 100 && 1 >> 2 ^ 3 * 4 ~ 5 % 6
}
